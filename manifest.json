{
  "schemaVersion": 1,
  "id": "io.github.zedster07.desktop-agent",
  "name": "Desktop Agent",
  "version": "3.0.0",
  "author": "Zedster07",
  "license": "MIT",
  "description": "Speak to your desktop and have it act, on a leash you own. Adds spoken commands on top of Omarchy's built-in Voxtype dictation: a phrase is matched against a declared intent registry, gated by a default-deny policy with an approval overlay and an audit trail. Other plugins can register their own voice commands.",
  "kinds": [
    "service",
    "panel",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "panel": "Panel.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Desktop Agent",
    "description": "Spoken commands, an approval overlay, and policy-gated agent control.",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "openSection": "status"
    },
    "schema": [
      {
        "key": "openSection",
        "type": "enum",
        "label": "Panel opens on",
        "options": [
          "status",
          "voice",
          "ai",
          "policy"
        ],
        "defaultValue": "status",
        "description": "Which section the panel shows when you click the bar icon. Everything else \u2014 speech engine, API key, AI assistance, policy \u2014 is configured inside the panel itself, so there is only one place to change a setting."
      }
    ]
  }
}